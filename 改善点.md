# connec+a プロジェクト 脆弱性・改善点レポート

本プロジェクト（connec+a）のコードベース全体（HTML、JavaScript、CSS、Google Apps Script）を調査した結果、検出されたセキュリティ脆弱性およびコード品質上の改善点の一覧です。

---

## 🚨 セキュリティ脆弱性（重要度：高〜中）

### 1. DOM-based XSS (クロスサイトスクリプティング) の脆弱性（重要度：高）
* **該当箇所**: 
  * [main.js:77-82](file:///c:/Users/nohr7/Documents/GitHub/connecta1/main.js#L77-L82) (`loadNews` 内のお知らせ挿入処理)
  * [main.js:97-106](file:///c:/Users/nohr7/Documents/GitHub/connecta1/main.js#L97-L106) (`loadNoteArticles` 内のnote記事挿入処理)
* **詳細**: 
  GAS (Google Apps Script) および note API から取得した外部データ（タイトルやリンクURL）をHTMLエスケープせずに `innerHTML` に直接挿入しています。
* **リスク**:
  スプレッドシートやnoteのタイトルに悪意あるHTML/JavaScriptコード（例: `<img src=x onerror=alert(1)>`）が書き込まれたり、リンク先に `javascript:〜` スキームのURLが指定された場合、サイト閲覧者のブラウザ上で不正なスクリプトが実行される危険性があります。
* **対策案**:
  * 文字列をHTMLエスケープするヘルパー関数を導入し、`innerHTML` に変数を埋め込む際は必ずエスケープを施す。
  * リンク先URLには `http://` または `https://` で始まっているかのバリデーションを行う。

### 2. お問い合わせフォーム（GAS）におけるスパム・メール送信機能の乱用（重要度：中）
* **該当箇所**: 
  * [gas/webapp.js:98-110](file:///c:/Users/nohr7/Documents/GitHub/connecta1/gas/webapp.js#L98-L110) (`doPost` 関数)
* **詳細**: 
  GASの `doPost` 関数において、受け取ったデータのバリデーション（必須項目の有無、メールアドレス形式の検証など）が一切行われていません。また、このWeb APIは公開（全員アクセス可）に設定されています。
* **リスク**:
  攻撃者がGASのデプロイURLへ直接リクエストを送るスクリプト（bot等）を作成した場合、無制限にお問い合わせメールを送信（スパム悪用）される可能性があります。Gmailの送信数上限を消費させられたり、メールボックスがスパムで埋まってしまうリスクがあります。
* **対策案**:
  * GASの `doPost` 内で、名前・本文などの必須チェックや、メールアドレスの形式チェック（正規表現）を追加する。
  * クライアント側（フロント）とサーバー側（GAS）で送信制限（簡易トークンの埋め込みなど）を検討する。

### 3. スプレッドシートIDのハードコーディング（重要度：中）
* **該当箇所**: 
  * [gas/webapp.js:28](file:///c:/Users/nohr7/Documents/GitHub/connecta1/gas/webapp.js#L28) (`SPREADSHEET_ID` 定数)
* **詳細**: 
  接続先スプレッドシートのIDが、ソースコード内に平文で直接記述されています。
* **リスク**:
  GitHub等のリポジトリがパブリック公開されている場合、スプレッドシートIDが全世界に公開されます。スプレッドシートの共有設定が「リンクを知っている全員が閲覧可能」などになっていると、内部データを不正に読み取られる恐れがあります。
* **対策案**:
  * GASの「スクリプトプロパティ（PropertiesService）」機能を使用し、コード上ではなくGASの環境設定からスプレッドシートIDを取得するように変更する。

---

## 🛠️ コード品質・品質上の改善点（重要度：低）

### 1. グローバルスコープの汚染
* **該当箇所**: 
  * [main.js](file:///c:/Users/nohr7/Documents/GitHub/connecta1/main.js) 全般
* **詳細**: 
  `currentPage`, `goPage`, `activeBiz` などの変数や関数がすべてグローバル空間（`window` オブジェクト）に定義されています。
* **リスク**: 
  他のJavaScriptライブラリと衝突したり、コンソールから意図しない状態の書き換えが発生しやすくなります。
* **対策案**: 
  * 即時実行関数（IIFE）やESモジュールを導入し、不要なグローバル変数を排除してカプセル化する。

### 2. エラーハンドリングの不足
* **該当箇所**: 
  * [main.js:107](file:///c:/Users/nohr7/Documents/GitHub/connecta1/main.js#L107) (`catch(e) {}`)
* **詳細**: 
  note記事の取得失敗時などのエラー処理が空になっており、例外が発生してもそのまま何も起きずに黙殺されます。
* **対策案**: 
  * 最低限コンソールにエラーを出力（`console.error`）し、画面側にも「記事の読み込みに失敗しました」などのフォールバック表示を行う。

### 3. SEOおよびアクセシビリティ対応の強化
* **該当箇所**: 
  * [index.html](file:///c:/Users/nohr7/Documents/GitHub/connecta1/index.html)
* **詳細**: 
  * 検索エンジン向けの `description` メタタグや、SNSシェア用の OGP メタタグ（`og:title` 等）が設定されていません。
  * `div` や `span` の多用によりセマンティックなHTML構造になっていません。
  * モバイルナビゲーションボタン（`nav-hamburger`）に、音声読み上げソフトやクローラー向けの `aria-label` や `aria-expanded` 属性がありません。
* **対策案**: 
  * メタタグやOGPタグを追加する。
  * ハンバーガーボタンに適切な `aria` 属性を設定し、JavaScriptから動的に切り替える。

### 4. CSSの未使用スタイル整理と変数の適用
* **該当箇所**: 
  * [style.css](file:///c:/Users/nohr7/Documents/GitHub/connecta1/style.css)
* **詳細**: 
  * `.biz-card` (169行目) や `.grid-6` (41行目) など、HTML側で使われていない古いスタイル定義が残っています。
  * CSS変数 `--bg` (5行目) が定義されているにもかかわらず、`body` などの背景指定で直接値がハードコードされています。
* **対策案**: 
  * 不要なCSSクラスをクリーンアップし、一貫してCSS変数を使用するように記述を変更する。
