# connec+a プロジェクト 脆弱性・改善点レポート（検証・対応済み）

本レポートは初版の指摘内容を実コードと照合し、**事実誤認を訂正したうえで対応状況を反映**したものです。
初版には誤った行番号、実在しない問題、実行するとサイトが壊れる対策案が含まれていたため、それらを明記しています。

対応日: 2026-07-11

---

## 🚨 セキュリティ

### 1. 外部データの未エスケープによるHTMLインジェクション ✅ 対応済み
* **該当箇所**: `main.js` の `loadNews` / `loadNoteArticles`
  * ※初版は「main.js:77-82 / 97-106」としていたが、**実際は 85-90 行目と 104-113 行目**だった。
* **詳細**:
  GAS（スプレッドシート）および note API から取得したデータを、HTMLエスケープせずテンプレートリテラルで `innerHTML` に埋め込んでいた。
* **重要度の訂正（高 → 中）**:
  初版は「重要度：高」としていたが、データ源は**自団体が管理するスプレッドシートと自団体のnoteアカウント**であり、外部の攻撃者が任意の値を書き込める経路ではない。したがって不特定の閲覧者が攻撃される実リスクは低い。
  ただし以下の実害があるため対応した。
  * `A&B` や `〈重要〉` のような**正当なタイトルで表示が壊れる**（こちらの方が現実的な問題）
  * スプレッドシートの編集権限を持つメンバーが多く、誤操作・内部リスクが残る
  * `cat-${n.category}` はクラス**属性**に生値を入れており、属性から脱出できる形だった
  * `a.thumb` は `style="background-image:url('...')"` の内側に生値が入り、CSSインジェクション経路になっていた
* **実施した対策**:
  * `escapeHtml()` ヘルパーを導入し、外部由来の値をすべてエスケープ
  * `safeUrl()` を導入し、`http://` / `https://` 以外（`javascript:` 等）は不採用。お知らせは `<a>` ではなく `<span>` で描画し、note記事はカードごとスキップ
  * カテゴリは style.css が定義する4種（イベント/メディア/採用/お知らせ）の**ホワイトリスト照合**にし、未知の値は `お知らせ` にフォールバック
  * `new Date()` が Invalid Date のときに「Invalid Date」と表示されるバグも同時に修正

### 2. お問い合わせフォーム（GAS）のバリデーション欠如・スパム悪用 ✅ 対応済み
* **該当箇所**: `gas/webapp.js` の `doPost`（初版の「98-110」は誤り。実際は 111-123 行目だった）
* **詳細**: バリデーションが一切なく、Web APIは「全員アクセス可」で公開されているため、GASのURLへ直接POSTすれば無制限にメール送信できた。
* **実施した対策**:
  * `postData` の存在チェック、必須項目チェック、メールアドレスの形式チェック（正規表現）
  * 文字数上限（name 100 / email 254 / subject 200 / message 5000）
  * **件名のヘッダインジェクション対策**（`\r` `\n` を除去）— 初版が見落としていた問題
  * **流量制限**: `CacheService` による 1時間あたり10通の上限。GASの `doPost` はクライアントIPを取得できないためIP単位の制限は不可能で、全体流量で Gmail の送信数上限を保護する
  * **ハニーポット**: フォームに視覚的に隠した `website` 項目を追加。bot が埋めた場合は送信せず成功を装う

### 3. スプレッドシートIDのハードコーディング ✅ 対応済み（ただし要手作業）
* **該当箇所**: `gas/webapp.js:28` の `SPREADSHEET_ID` 定数
* **リスク評価の訂正**:
  初版は「内部データを不正に読み取られる恐れ」としているが、**スプレッドシートIDは秘密鍵ではない**。共有設定が非公開であれば、IDを知られただけでは閲覧できない。
* **実施した対策**:
  * `PropertiesService.getScriptProperties()` から取得するよう変更し、コードからIDを削除
* **⚠️ 残っている手作業（必須）**:
  1. GASエディタ →「⚙ プロジェクトの設定」→「スクリプト プロパティ」に `SPREADSHEET_ID` を登録し、**再デプロイ**する（これをしないとお知らせが表示されなくなる）
  2. **IDはgit履歴に残るため、過去のコミットからは依然として読める。** publicリポジトリである以上、対象スプレッドシートの共有設定が「リンクを知っている全員が閲覧可」になっていないかを Google Drive 上で必ず確認すること。**これが実質的な対策で、コード変更はその補助にすぎない。**

---

## 🛠️ コード品質・アクセシビリティ

### 1. グローバルスコープの汚染 ❌ 対応しない（初版の対策案は危険）
* **事実確認**: `currentPage` / `goPage` / `activeBiz` 等がグローバルにあるのは事実。
* **しかし初版の「IIFE / ESモジュール化」をそのまま実行するとサイトが壊れる**:
  `index.html` は `onclick="goPage(0)"` `onclick="openIssue(0)"` `onclick="showBiz(0)"` `onsubmit="submitContact(event)"` といったインラインハンドラに全面依存しており、関数をグローバルから隠すと**全ボタンが無反応になる**。
* **判断**: 実施には全ハンドラの `addEventListener` 化という大規模リファクタが必要。一方このサイトは**外部JSライブラリを1つも読み込んでいない**（`main.js` のみ）ため、名前衝突の実リスクはゼロ。費用対効果が見合わないため**見送る**。

### 2. エラーハンドリング ✅ 一部対応（初版の記述は誤り）
* **初版の「`catch(e) {}` が空で例外が黙殺される」は事実誤認**。実際には画面へのフォールバック表示（「お知らせを読み込めませんでした」／noteセクション非表示）が既に実装されていた。
* **実施した対策**: 既存のフォールバックは維持し、デバッグ性のため `console.error()` を追加したのみ。

### 3. SEO・アクセシビリティ ✅ 対応済み（初版に報告漏れあり）
* **初版が見落としていた重大な問題**:
  * **`<h1>` がページ内に1つも存在しなかった**（ヒーローのロゴが `div`+`span`）。description欠落より深刻なSEO上の欠陥。
  * **`issue-node` / `penta-node` が `div` + `onclick` のため、キーボードで一切操作できなかった。**
* **実施した対策**:
  * `description` / `canonical` / OGP（`og:title` `og:description` `og:url` `og:image` 等）/ `twitter:card` / `theme-color` を追加
  * ヒーローロゴを `<h1>` 化（`.hero-logo` の子要素が font-size を明示しているため見た目は不変）
  * 全ページを `<main>` ランドマークで囲む
  * ハンバーガーボタンに `aria-label` / `aria-expanded` / `aria-controls` を付与し、JSから `aria-expanded` を同期
  * クリック可能な `div` に `role="button"` `tabindex="0"` `aria-label` を付与し、Enter/Space で発火する共通 keydown ハンドラを追加（`<button>` への置換はCSSの絶対配置レイアウトを壊すため採らなかった）
  * `images/leader.jpg` に `width` / `height` / `loading="lazy"` を追加（CLS対策）
* **⚠️ 残っている手作業**: SNSシェア用画像 `images/ogp.png`（1200×630px）が未配置。配置するまでSNSでは画像なし表示になる。
* **今回対象外（構造上の制約）**: ページ切替がJSベースでURLが変わらないため、各ページが個別に検索エンジンへインデックスされない。解消には大規模改修が必要。

### 4. CSSの未使用スタイルと変数の未適用 ✅ 対応済み
* **実施した対策**（すべて未使用であることを grep で確認済み。見た目は不変）:
  * `.biz-card` / `.biz-icon` / `.biz-head` 系ブロックを削除。`/* BIZ (kept for detail) */` というコメントは誤りで、事業詳細パネルは実際には `.penta-detail` が担当していた
  * `.grid-6` を削除（`.grid-sm` は使用中のため残置）
  * `.card-dark` **クラス**を削除（初版が見落としていた未使用スタイル）。ただし**変数 `--card-dark` は hero/footer で使用中のため残置**
  * `body` の背景 `#ececf0` を `var(--bg)` に統一
