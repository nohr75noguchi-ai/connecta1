# お知らせ欄 GAS連携 実装報告

---

## 作業概要

TOPページの「お知らせ」欄を、静的ファイル（`data/news.json`）管理からGoogleスプレッドシート管理に移行した。
スプレッドシートに行を追加するだけでサイトに反映されるようになった。

---

## 変更したファイル

| ファイル | 変更内容 |
|---|---|
| `gas/webapp.js` | スプレッドシートからお知らせを返す`?type=news`ルートを追加 |
| `main.js` | `loadNews()`の取得先を`data/news.json`→`GAS_URL?type=news`に変更 |

---

## 仕様

### スプレッドシート構成

シート名は **「お知らせ」**（1行目はヘッダー）

| 列 | 内容 | 例 | 備考 |
|---|---|---|---|
| A | 日付 | 2026/06/26 | Googleの日付セル形式でも可 |
| B | カテゴリ | イベント | 下記4種類から選ぶ |
| C | タイトル | ○○イベント開催！ | 必須 |
| D | URL | https://note.com/... | 任意。空欄可 |

**カテゴリ一覧（色付きバッジとして表示）**

| カテゴリ名 | バッジ色 |
|---|---|
| イベント | 青 |
| メディア | オレンジ |
| 採用 | 緑 |
| お知らせ | グレー |

### 表示ルール

- 日付の降順（新しい順）で自動ソートされる
- URLがある場合 → タイトルがリンクになり、別タブで開く
- URLが空欄の場合 → タイトルはただのテキスト
- GAS_URLが未設定の場合 → 「お知らせはありません」を表示

### GASのルーティング

| リクエスト | 動作 |
|---|---|
| GET `?type=news` | スプレッドシートからお知らせを返す |
| GET（パラメータなし） | note.com記事を返す（従来通り） |
| POST | お問い合わせフォームのメール送信（従来通り） |

---

## TODO

- [ ] Googleスプレッドシートを新規作成し、シート名を「お知らせ」にする
- [ ] `gas/webapp.js` の `SPREADSHEET_ID` に作成したスプレッドシートのIDを貼り付ける
- [ ] GASをデプロイ（実行ユーザー：自分 / アクセス：全員）
- [ ] デプロイURLを `main.js` の `GAS_URL` に設定する（すでに設定済みならそのまま使える）
- [ ] スプレッドシートにお知らせのサンプルデータを入力して動作確認する

### スプレッドシートIDの確認方法

スプレッドシートのURLの以下の部分をコピーする：
```
https://docs.google.com/spreadsheets/d/【ここがID】/edit
```
