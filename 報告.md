# お知らせ欄 GAS連携 実装報告

---

## 作業概要

TOPページの「お知らせ」欄を、静的ファイル（`data/news.json`）管理からGoogleスプレッドシート管理に移行した。
スプレッドシートに行を追加するだけでサイトに反映されるようになった。

---

## 変更したファイル

| ファイル | 変更内容 |
|---|---|
| `gas/webapp.js` | スプレッドシートからお知らせを返す`?type=news`ルートを追加 |
| `main.js` | `loadNews()`の取得先を`data/news.json`→`GAS_URL?type=news`に変更 |

---

## 仕様

### スプレッドシート構成

シート名は **「お知らせ」**（1行目はヘッダー）

| 列 | 内容 | 例 | 備考 |
|---|---|---|---|
| A | 日付 | 2026/06/26 | Googleの日付セル形式でも可 |
| B | カテゴリ | イベント | 下記4種類から選ぶ |
| C | タイトル | ○○イベント開催！ | 必須 |
| D | URL | https://note.com/... | 任意。空欄可 |

**カテゴリ一覧（色付きバッジとして表示）**

| カテゴリ名 | バッジ色 |
|---|---|
| イベント | 青 |
| メディア | オレンジ |
| 採用 | 緑 |
| お知らせ | グレー |

### 表示ルール

- 日付の降順（新しい順）で自動ソートされる
- URLがある場合 → タイトルがリンクになり、別タブで開く
- URLが空欄の場合 → タイトルはただのテキスト
- GAS_URLが未設定の場合 → 「お知らせはありません」を表示

### GASのルーティング

| リクエスト | 動作 |
|---|---|
| GET `?type=news` | スプレッドシートからお知らせを返す |
| GET（パラメータなし） | note.com記事を返す（従来通り） |
| POST | お問い合わせフォームのメール送信（従来通り） |

---

## TODO

- [x] Googleスプレッドシートを新規作成し、シート名を「お知らせ」にする
- [x] `gas/webapp.js` の `SPREADSHEET_ID` に作成したスプレッドシートのIDを貼り付ける
- [x] GASをデプロイ（実行ユーザー：自分 / アクセス：全員）
- [x] デプロイURLを `main.js` の `GAS_URL` に設定する（すでに設定済みならそのまま使える）
- [x] スプレッドシートにお知らせのサンプルデータを入力して動作確認する
- [x] お知らせの日付表示を「2026年7月11日」形式に変更（月またぎでも正しくソートされるよう内部ソートキーを追加）
- [x] お問い合わせページの「メールでのお問い合わせ（mailto）」表示を削除し、フォームのみに統一
- [ ] お問い合わせメールのCC設定：`connecta.official@gmail.com` をCCに追加する件。To（主宛先）をどうするか未確定のため保留中（詳細は下記メモ参照）

### スプレッドシートIDの確認方法

スプレッドシートのURLの以下の部分をコピーする：
```
https://docs.google.com/spreadsheets/d/【ここがID】/edit
```

---

## 2026-07-11 作業ログ

### 1. お知らせの日付表示フォーマット修正

- 症状：スプレッドシートの日付セルが `Thu Jan 15 2026 00:00:00 GMT+0900 (日本標準時)` のような英語形式でサイトに表示されてしまう
- 原因：セルの値がDate型ではなく、その英語形式のテキストがそのまま入っていたため、`gas/webapp.js` の「文字列ならそのまま使う」分岐を通ってしまっていた
- 対応：`getNews()` を修正し、Date型でなくても `new Date(raw)` で解釈できる文字列は自動でDate変換して `yyyy年M月d日`（例：2026年7月11日）形式に整形するようにした。表示用の日付文字列とは別に、ソート用の内部キー（`yyyyMMdd`形式）を持たせることで、月をまたいでも新しい順ソートが崩れないようにした
- デプロイ後に動作確認済み、正常に「2026年7月11日」形式で表示されることを確認

### 2. お問い合わせページの「メールでのお問い合わせ」表示を削除

- `index.html` のお問い合わせページから「メールでのお問い合わせ：connecta.official@gmail.com」というmailtoリンクの一文を削除
- フォーム（`#contactForm`）のみを残す構成に変更

### 3. お問い合わせメールのCC設定（保留・要確認）

- 依頼内容：GASの `doPost`（お問い合わせフォームのメール送信）で、`connecta.official@gmail.com` にもCCで届くようにしたい
- 論点：現状すでに `CONTACT_EMAIL = 'connecta.official@gmail.com'`（`gas/webapp.js:26`）がTo（主宛先）に設定されているため、同じアドレスをCCに追加すると宛先が重複する
- 確認待ち：Toを別のアドレスに変更した上でconnecta.official@gmail.comをCCにするのか、それとも別の意図があるのか、ユーザーへ確認中
- 対応：方針が決まり次第、`gas/webapp.js` の `doPost` / `GmailApp.sendEmail` にCC設定を追加する
