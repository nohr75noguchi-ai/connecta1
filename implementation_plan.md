# Webアプリケーションの脆弱性・改善点に関する調査結果と修正計画

このプロジェクト（connec+a）のコードベース全体を確認した結果、セキュリティ上の重大な脆弱性が複数見つかりました。また、コードの保守性や品質、アクセシビリティの観点でもいくつかの改善点があります。

以下に調査結果と、それらを解決するための実装計画を提案します。

---

## 🚨 セキュリティ脆弱性 (重大度: 高〜中)

### 1. 外部データ読み込み時の DOM-based XSS (重大度: 高)
* **対象コード**: 
  * [main.js:77-82](file:///c:/Users/nohr7/Documents/GitHub/connecta1/main.js#L77-L82) (`loadNews` 内のお知らせ表示処理)
  * [main.js:97-106](file:///c:/Users/nohr7/Documents/GitHub/connecta1/main.js#L97-L106) (`loadNoteArticles` 内のnote記事表示処理)
* **原因**: 
  GAS (Google Apps Script) および note API から取得した外部データ（タイトル、リンクURL等）をエスケープせずに、そのまま `innerHTML` を使ってHTML構造に流し込んでいます。
* **リスク**:
  スプレッドシートのお知らせデータやnote記事の情報に、悪意ある第三者がJavaScriptを含むタグ（例: `<img src=x onerror=alert(1)>`）や、`javascript:alert(1)` のようなリンクを書き込んだ場合、サイト閲覧者のブラウザ上で悪意あるスクリプトが実行され、個人情報の窃取や不正な遷移が行われる危険性（クロスサイトスクリプティング）があります。
* **対策計画**:
  * HTMLエスケープ処理を行うユーティリティ関数を `main.js` に追加し、HTMLを構築するすべての箇所でテキストをエスケープします。
  * `a href="..."` に渡すリンクURLについては、`javascript:` スキームを排除するURLバリデーション（`http:` または `https:` で始まることの確認）を適用します。

### 2. お問い合わせフォーム（GAS側）のスパム送信・メール送信機能の乱用リスク (重大度: 中)
* **対象コード**:
  * [gas/webapp.js:98-110](file:///c:/Users/nohr7/Documents/GitHub/connecta1/gas/webapp.js#L98-L110) (`doPost` 関数)
* **原因**:
  GAS側で入力データのバリデーション（名前・メール・本文の存在チェック、メールアドレスの形式検証）が一切行われていません。また、このAPIは外部の誰でも無制限に呼び出すことができます。
* **リスク**:
  攻撃者がGASの公開デプロイURLを直接叩くことで、自動化されたスクリプト（bot）を使って大量の迷惑メールを `CONTACT_EMAIL` に送信できます。これにより、Gmailの1日の送信制限枠を消費させられたり、メールボックスがスパムで埋まったりするDoS（サービス拒否）攻撃を受けるリスクがあります。
* **対策計画**:
  * JavaScript側（フロントエンド）とGAS側（バックエンド）の両方で厳密な入力値検証（バリデーション）を実装します。
  * GASの `doPost` 内で必須パラメータのチェック、メールアドレスの正規表現チェックを行い、不正なデータは処理しないようにします。
  * 簡易的なスパム防止策として、フロントエンドでフォーム送信時にタイムスタンプなどの簡易トークンを付与し、GAS側で送信所要時間やトークン検証を行うロジック、あるいは送信頻度を制限する仕組みを追加します。

### 3. スプレッドシートIDのハードコーディング (重大度: 中)
* **対象コード**:
  * [gas/webapp.js:28](file:///c:/Users/nohr7/Documents/GitHub/connecta1/gas/webapp.js#L28) (`SPREADSHEET_ID` 変数)
* **原因**:
  GASのスクリプトファイル内に、操作対象のスプレッドシートIDが平文で直接書き込まれています。
* **リスク**:
  このGitHubリポジトリが一般公開（Public）されている場合、スプレッドシートのIDが全世界に公開されてしまいます。アクセス権限の設定が「誰でも閲覧可能」になっている場合、スプレッドシート内の全てのデータが第三者に盗取される可能性があります。
* **対策計画**:
  * スプレッドシートIDをソースコード上に直書きせず、GASの「スクリプトプロパティ（環境変数のような機能）」から動的に取得するように変更します。これにより、GitHubにコミットするコードからIDを安全に隠蔽できます。

---

## 🛠️ コード品質・品質上の改善点 (重大度: 低)

### 1. グローバルスコープの汚染
* **対象コード**: [main.js](file:///c:/Users/nohr7/Documents/GitHub/connecta1/main.js)
* **原因**: 
  `currentPage`, `goPage`, `submitContact` などの変数や関数が、すべてブラウザのグローバル空間（`window` オブジェクト）に定義されています。
* **リスク**:
  他の外部スクリプトを追加した際に変数名が衝突したり、デベロッパーツール等から意図しない状態変更が行われたりしやすくなります。
* **対策計画**:
  * 即時実行関数（IIFE）やオブジェクト指向カプセル化、またはESモジュールの導入により、グローバルスコープをきれいに整理します。

### 2. エラーハンドリングの黙殺
* **対象コード**: [main.js:107](file:///c:/Users/nohr7/Documents/GitHub/connecta1/main.js#L107) (`catch(e){}`)
* **原因**:
  note記事の取得に失敗した場合のエラーハンドリングが空（何も処理しない）になっています。
* **対策計画**:
  * コンソールにエラーログ（`console.error`）を出力するようにし、さらに画面上に「記事を読み込めませんでした」などの一時的なフォールバックメッセージを表示するユーザー体験の向上を行います。

### 3. HTML/SEOの最適化とアクセシビリティ対応
* **対象コード**: [index.html](file:///c:/Users/nohr7/Documents/GitHub/connecta1/index.html)
* **原因・課題**:
  * 検索エンジン向けの `description` メタタグや、SNSシェア時の見栄えを整える OGPメタタグ（`og:title`, `og:image` 等）が未設定です。
  * `div` や `span` が多用されており、セマンティックなHTMLタグ（`<section>`, `<article>`, `<header>` 等）が十分に活用されていません。
  * モバイル用のハンバーガーボタンにアクセシビリティ用の属性（`aria-label="メニューを開く"` や `aria-expanded`）が欠如しています。
* **対策計画**:
  * メタタグ、OGPタグの追加。
  * マークアップのセマンティック化。
  * ボタンへの `aria` 属性の付与。

### 4. CSSの未使用スタイル整理と変数の活用
* **対象コード**: [style.css](file:///c:/Users/nohr7/Documents/GitHub/connecta1/style.css)
* **原因・課題**:
  * `.biz-card` や `.grid-6` など、HTMLで使用されていない不要なクラスが残っています。
  * CSS変数 `--bg` などが定義されていますが、実際の要素（`body` 等）で直接カラーコードがハードコードされています。
* **対策計画**:
  * 未使用スタイルの削除と整理。
  * CSS変数を一貫して使用する形に修正。

---

## 📋 提案する具体的な変更箇所

### GAS側 ([webapp.js](file:///c:/Users/nohr7/Documents/GitHub/connecta1/gas/webapp.js))
* `var SPREADSHEET_ID` の取得を `PropertiesService.getScriptProperties().getProperty('SPREADSHEET_ID')` に変更（なければ元のIDをフォールバックとして保持）。
* `doPost(e)` に入力バリデーション（必須項目、メールフォーマット）を追加。

### フロントエンドJS ([main.js](file:///c:/Users/nohr7/Documents/GitHub/connecta1/main.js))
* HTMLエスケープ用のヘルパー関数 `escapeHTML(str)` と URL安全検証用の `sanitizeURL(url)` の導入。
* `loadNews()` および `loadNoteArticles()` における文字列の結合部分でエスケープを適用。
* `submitContact()` にクライアントサイドのバリデーションを追加。
* エラーハンドリング時のUI表示とログ出力を追加。

### HTML ([index.html](file:///c:/Users/nohr7/Documents/GitHub/connecta1/index.html))
* メタタグ（Description、OGP関連）を追加。
* ハンバーガーボタンに `aria-label` および `aria-expanded` を追加し、`main.js` から動的に制御するよう調整。

---

## 🧪 検証計画

### 1. 自動テスト / ローカル検証
* ローカル環境でブラウザを開き、`loadNews()` および `loadNoteArticles()` が正常にデータを読み込んで安全に表示できるか確認する。
* お問い合わせフォームに不正なメールアドレスや空のデータを入力して送信し、クライアント/サーバー双方でエラーメッセージが表示されることを確認する。

### 2. 手動テスト項目
* スプレッドシートデータにテストデータ（HTMLタグ、`javascript:` スキーム付きURL等）を意図的に挿入し、XSSが発生せず正しくエスケープされたテキストとして表示されるか、またはリンクが無効化されるかを確認する。
* モバイル表示時のハンバーガーメニューの挙動確認。
